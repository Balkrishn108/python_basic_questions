# Write a Python program to find the area of a triangle.
height = float(input("Enter height of tringle : "))
base = float(input("Enter base of tringle : "))

area = 0.5 * base * height
print(f"Area of triangle {area} square unit")